<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Trazadores Cúbicos</title>
	</head>
	<body>
		<slot />
		
		
		<!-- Footer con marca de agua -->
		<footer class="watermark-footer">
			<div class="watermark-left">
				<span class="watermark-text text1"><b>Universidad Nacional de Ingeniería</b></span>
			</div>
			<div class="watermark-center">
				<span class="watermark-text">Métodos Numéricos 2024</span>
			</div>
			<div class="watermark-right">
				<span class="watermark-text text3">Elias Ortega</span>
			</div>
		</footer>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		position: relative;
	}
	
	/* Footer con marca de agua */
	.watermark-footer {
		position: fixed;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 60px;
		background: linear-gradient(135deg, rgba(0, 123, 255, 0.1) 0%, rgba(0, 123, 255, 0.05) 100%);
		backdrop-filter: blur(10px);
		border-top: 1px solid rgba(0, 123, 255, 0.2);
		display: flex;
		align-items: center;
		justify-content: space-between;
		z-index: 1000;
		opacity: 0.8;
		transition: opacity 0.3s ease;
		padding: 0 20px;
	}
	
	.watermark-footer:hover {
		opacity: 1;
	}
	
	.watermark-left {
		display: flex;
		justify-content: flex-start;
	}
	
	.watermark-center {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 20px;
	}
	
	.watermark-right {
		display: flex;
		justify-content: flex-end;
		margin-right: 50px;
	}
	
	.watermark-text {
		font-weight: 500;
		color: rgba(0, 123, 255, 0.7);
		font-family: 'Arial', sans-serif;
		font-size: 14px;
		letter-spacing: 1px;
	}
	
	/* Asegurar que el contenido principal no se superponga con el footer */
	body > *:not(.watermark-footer) {
		margin-bottom: 60px;
	}
</style>
